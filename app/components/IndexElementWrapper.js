import styled from 'styled-components/native';

import { colors } from '../config';

const IndexElementWrapper = styled.View`
  background-color: ${colors.white};
  flex-direction: row;
  padding-horizontal: 15;
  padding-vertical: 10;
`;

export default IndexElementWrapper;
