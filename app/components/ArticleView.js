import styled from 'styled-components/native';
import { colors } from '../config';

const ArticleView = styled.ScrollView`
  background-color: ${colors.lighterGray};
  flex: 1;
`;

export default ArticleView;
