import styled from 'styled-components/native';

import { colors } from '../config';

const IndexItemSeparator = styled.View`
  background-color: ${colors.darkerGray};
  height: 0.85;
`;

export default IndexItemSeparator;
