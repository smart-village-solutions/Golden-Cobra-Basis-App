import { StyleSheet } from 'react-native';

import colors from '../colors';

export default StyleSheet.create({
  text: {
    color: colors.black,
    fontWeight: '400'
  },
  link: {
    color: colors.green,
    fontWeight: '400'
  }
});
